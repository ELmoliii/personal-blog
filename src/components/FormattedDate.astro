---
interface Props {
	date: Date;
}

const { date } = Astro.props;
//const mesesAbreviadosEN = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
---

<time datetime={date.toISOString()}>
  {date.toLocaleDateString('es-ES', {
    year: 'numeric',
    month: 'short',
    day: 'numeric'   

  })}
</time>

